{{!-- {{ blogPost }} --}}

<div class="flex-container card blog-post">
  <div class="card-body">
    <div class="row">
      <div class="col-6 title">
        <h2>{{post.title}}</h2>
      </div>
      <div class="col-6 name-date">
        <p>{{post.username}} {{post.dateCreated}}</p>
      </div>
    </div>
    <div class="row">
      <div class="col-12 content">
        <p>{{post.content}}</p>
      </div>
    </div>
  </div>
</div>

{{!-- {{/blogPost}} --}}

<div class="card">
  <div class="card-body">
    <div>
      <label>Comment</label>
      <input type="input" />
      <button>Confirm</button>
    </div>
  </div>
</div>

{{#each comment}}

  <div class="card">
    <div class="card-body">
      <div>
        <span>Comments</span>
      </div>
      <div class="prev-comments">
        {{text}}
      </div>
    </div>
  </div>

{{/each}}